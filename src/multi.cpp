#include <iostream>
#include "multi.h"
#include "multi.1.h"

using std::cin, std::cout, std::endl;

int main () {
  std::ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  C<233> c;
  volatile int x = c.f();
  C<1926> c1;
  f();
  return 0;
}
